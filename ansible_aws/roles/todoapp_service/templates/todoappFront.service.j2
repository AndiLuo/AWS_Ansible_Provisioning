[Unit]
Description=Todo app, ACIT4640
After=network.target

[Service]
Environment=NODE_PORT={{todoapp_port}}
WorkingDirectory={{todoapp_path_front}}
Type=simple
User={{todo_user}}
ExecStart=/bin/npm start
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target