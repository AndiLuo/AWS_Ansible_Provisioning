[Unit]
Description=Todo app, ACIT4640
After=network.target

[Service]
WorkingDirectory=/home/{{todo_user}}/app
Type=simple
User={{todo_user}}
ExecStart=/usr/bin/node {{todoapp_path_back}}/server.js
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target